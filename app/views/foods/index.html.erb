<div class="center container-fluid">	
	<h1>Some Website Name</h1>
	
	<div class="container-fluid">
		<div class="col-md-12">
			<select id="company_name" name="company_name" class="search-form-select-list-big">
			<option value="" disabled selected>Select A Restaurant</option>
			<% Food.distinct_company_names.each do |name| %>
				<option value="<%= name.company_name %>"><%= name.company_name %></option>
			<% end %>
			</select>
		</div>
		
		<h2>Or</h2>
		
		<div class="col-md-12">
			<form action="/search" method="get"><input class="btn-search-big" type="submit" value="Search All" /></form>
		</div>
		
	</div>
</div>

<script>
  $('#company_name').on('change', function() {
    if ($(this).val().length) {
      window.location = '/restaurant/' + $(this).val()
    }
  })
</script>